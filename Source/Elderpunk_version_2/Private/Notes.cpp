#include "Notes.h"
/*
bool isExistNoteByTitle(std::string title) {
	return Notes::save.contains(title);
}

void addNote(std::string title, FString desc) {
	if (isExistNoteByTitle(title)) return;

	//Notes::save.insert(std::make_pair(title, desc));
}

void editNote(std::string title, FString desc) {
	if (!isExistNoteByTitle(title)) return;

	//Notes::save.
}
*/